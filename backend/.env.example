PORT=4000
# Required in production. Sign/verify patient tokens. Generate: openssl rand -hex 32
JWT_SECRET=change-me
GRACE_MINUTES=30
TICK_SECONDS=60
# DATA_FILE=/data/data.json   # set to a volume path when deployed
