# MediMate Backend

Placeholder for the **Phase-B caregiver relay**: Web Push subscriptions, caregiver share
links, and missed-dose alerts. Deliberately empty until the caregiver wedge is validated
with real users — see `../docs/briefs/brief-medimate-2026-07-20/`.

Zero dependencies; TypeScript runs natively on Node ≥ 22.18.

```bash
npm run dev   # http://localhost:4000/health
```
